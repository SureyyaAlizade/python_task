#Task 1
'''
from random import*
def matrix(n, m):
    A = []
    for i in range(n):
        A += [[0]*m]
        for j in range(m):
            A[i][j] = randint(10,100)

    return A

def print_mat(A):
    for row in A:
        for i in row:
            print(f'{i:4}', end = '')

        print()

def ebob(a, b):
    ebob = 1
    for i in range(1,a+1):
        if a%i == 0 and b%i == 0:
            ebob = i

    return ebob
    

n = int(input('Enter n: '))
A = matrix(n, n)
print_mat(A)
max_ = A[0][0]
min_ = A[0][0]

for i in range(n):
    for j in range(n):
        if j%2 == 0:  #tek nomreli sutun indexi cutdur
            if max_ < A[i][j]:
                max_ = A[i][j]

        if i%2 == 1:
            if min_ > A[i][j]:
                min_ = A[i][j]

print(max_, min_, ebob(max_,min_))
'''

#Task 2
'''
ls = [int(x) for x in input().split()]
print(ls)
unique = []

for i in ls:
    count = 0
    for _ in ls:
        if _ == i:
            count += 1

    if count == 1:
        unique += [i]


print(unique)
first_unique = unique[0]
for i in range(1, first_unique):
    if first_unique%i == 0:
        div = i

print(first_unique, div)

'''
'''
#Task 3
string = input('ENter string: ')
count = 1
n = len(string)
i = 0
new = ''
while i < n-1:
    if string[i] == ' ':
        new += '-'
        i += 1
        continue

    if '0' <= string[i] <= '9':
        new += '(' + string[i] + ')'
        i += 1
        continue

    if string[i] == string[i+1]:
        count += 1

    else:
        if count != 1:
            new += string[i] + str(count)
            count = 1
        else:
            new += string[i]
        
    i+= 1

if string[n-1] ==' ':
    new += '-'
elif '0' <= string[n-1] <= '9':
    new += '(' + string[n-1] + ')'

else:
    new += string[n-1]
    
print(new)
        
'''

'''        
#Task 4
num = int(input('Enter num: '))
tek = []
cut = []
while num > 0:
    d = num%10
    if d % 2 == 0:
        cut += [d]
    else:
        tek += [d]

    num //= 10

hasil = 1
cem = 0
for i in tek:
    cem += i
for i in cut:
    hasil *= i

print(hasil, cem)
if hasil > cem:
    print('Active')

else: print('Passive')
'''
'''
#Task 5
def samit(x):
    sait = 'aiuoeyAIOUEY'
    var = 0
    for _ in sait:
        if x == _:
            var = 1
            break

    return var == 0

def samit_say(x):
    l = 0
    for i in x:
        if samit(i):
            l += 1
    return l

def sait(x):
    sait = 'aiuoeyAIOUEY'
    var = 0
    for _ in sait:
        if x == _:
            var = 1
            break

    return var == 1

def sait_say(x):
    l = 0
    for i in x:
        if sait(i):
            l += 1
    return l



def ch(x):
    return 'a'<=x<='z' or 'A'<=x<='Z'

def my_split(x):
    i = 0
    word = ''
    n = len(x)
    ls = []
    while i < n:
        if ch(x[i]):
            word += x[i]

        else:
            if word != '':
                ls += [word]
                word = ''

        i += 1

    if word != '':
        ls += [word]

    return ls    
                

string = input('Enter string: ')
ls = my_split(string)
sait_ = 0
samit_ = 0
n = len(ls)
for i in range(n):
    for j in range(n-1):
        if samit_say(ls[j]) > samit_say(ls[j+1]):
            ls[j], ls[j+1] = ls[j+1], ls[j]

        elif samit_say(ls[j]) == samit_say(ls[j+1]):
            if sait_say(ls[j]) > sait_say(ls[j+1]):
                ls[j], ls[j+1] = ls[j+1], ls[j]

print(ls)
'''








